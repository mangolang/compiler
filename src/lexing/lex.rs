use crate::token::Tokens;
use crate::io::source::SourceFile;

//TODO @mark: one day maybe use arena allocation

/// Lexes a whole source file and returns the tokens.
pub fn lex(source: &SourceFile) -> Vec<Tokens> {

}
